# AEGIS environment configuration template.
# No provider-specific variables are committed until the corresponding
# Bright Data, model, runtime, and storage contracts are verified.
#
# Add only variables backed by a confirmed implementation decision.
