# AEGIS Mission 018 — Repair Mission 016 Preflight

**Date:** 2026-08-18
**Status:** **REPAIRED_VALIDATION_ONLY**
**Branch:** `mission-018-repair-floor-preflight`
**Execution boundary:** Mission 016 was not rerun.

## Executive result

Mission 018 repaired the Mission 016 preflight/smoke-test boundary so that a future Mission 016 rerun consumes the corrected Mission 017 benchmark freeze. The repair is isolated to configuration selection, expected revision identity, run-directory isolation, and historical-evidence checks.

Mission 018 did **not** run the repaired preflight entry point. It did not run the Baseline B Gemini smoke test, Gemini, Bright Data, healing, provider approval, production commit, rollback, benchmark, or metric calculation.

> The preflight is repaired; execution remains a separate future action. `READY_TO_EXECUTE` is a validation state, not benchmark authorization.

## Original Mission 016 failure

The historical Mission 016 preflight used the superseded Mission 015 freeze:

| Stale active value | Historical value |
| --- | --- |
| Configuration path | `benchmarks/configs/mission_015_frozen_config.json` |
| Configuration hash | `f48ec5c5792b09623b6b6e4bcab9da6b9c5066506a57e012826a3b837e8d7d96` |
| Baseline A revision | `0e8bcc4a2c2184cae9a50b291054ec47d83fc895` |
| Baseline B revision | `0e8bcc4a2c2184cae9a50b291054ec47d83fc895` |
| Historical run ID | `mission_016_floor_f48ec5c5792b` |

Mission 016 correctly stopped before the smoke test and benchmark because the frozen Baseline A/B revision did not resolve to the actual implementation commit. Its run directory is historical invalidation evidence and was not modified.

## Corrected active boundary

| Field | Corrected value |
| --- | --- |
| Configuration path | `benchmarks/configs/mission_017_corrected_frozen_config.json` |
| Configuration hash | `59a11e27a71f241dbf58d1d41bc37a53ba52b2652cbe23f7e2d46891c63e0f0b` |
| Baseline A revision | `0e8bcc4ea8c1bbcb7dae21b12ec1710366e39f47` |
| Baseline B revision | `0e8bcc4ea8c1bbcb7dae21b12ec1710366e39f47` |
| AEGIS revision | `7de2bc65ed9eeb9f4abd24017543f3f366990738` |
| New future run ID | `mission_016_floor_59a11e27a71f` |
| Historical run ID preserved | `mission_016_floor_f48ec5c5792b` |

The corrected preflight still checks the actual Git source revisions, configuration validity/hash, fixture, M001–M006, seed `12345`, participant readiness, fairness, `mission-010-metrics-v1`, artifact paths, immutable TEST_DOUBLE fixture state, and the normal validation-only dry-run status. It does not weaken or bypass the revision check.

## Historical evidence preservation

The directory `benchmarks/runs/mission_016_floor_f48ec5c5792b` remains untouched. The repaired boundary requires that directory to exist as historical evidence and separately requires the corrected future run directory to be absent before any future preflight run. No new run directory, smoke artifact, benchmark result, or report directory was generated by Mission 018.

Mission 015’s configuration and freeze records remain historical and unchanged. Mission 017 remains the superseding corrected freeze.

## Tests

The focused Mission 018 suite contains seven tests covering corrected configuration loading, rejection of the superseded hash, exact revision resolution, historical directory preservation, deterministic run-ID isolation, fail-closed old-config substitution, normal validation-only dry-run evaluation, and zero artifact side effects.

Focused result:

```text
7 passed
```

The full repository suite passed with `221 passed`:

```bash
PYTHONPATH=src pytest -q tests/unit tests/integration
```

## Safety counters

Mission 018’s final state is:

| Counter | Value |
| --- | ---: |
| `benchmark_runs_executed` | 0 |
| `provider_operations_executed` | 0 |
| `healing_operations_executed` | 0 |
| `metric_results_generated` | 0 |
| `execution_authorized` | false |
| Gemini smoke executed | false |
| Bright Data called | false |
| Historical Mission 016 directory modified | false |
| Benchmark result files generated | false |

## Exact future Mission 016 command

The repaired future command is:

```bash
PYTHONPATH=src python scripts/mission016_preflight_smoke.py
```

This command was **not executed** in Mission 018. A future run must stop before the Baseline B smoke step if any corrected preflight check fails. If preflight succeeds, the intended future sequence remains:

```text
corrected preflight → Baseline B smoke → 180-run benchmark floor
```

Mission 018 stops here. It does not authorize or begin Mission 016.
